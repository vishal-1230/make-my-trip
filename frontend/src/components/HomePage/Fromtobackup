<div className="fromtodiv">
        <div>
          <h3 className="location-heading">LOCATION</h3>
          <select className="select-heading" onChange={handleChange} name="from" id="">
            {text.map((e) => (
              <option className="list-options" value={e.IATA_code} key={e.IATA_code}>
                {e.city_name}
              </option>
            ))}
          </select>
        </div>
        {/* <div>
          <h3>TO</h3>
          <select onChange={handleChange} name="to" id="">
            {text.map((e) => (
              <option value={e.IATA_code} key={e.IATA_code}>
                {e.city_name}
              </option>
            ))}
          </select>
        </div> */}
      </div>
      <div className="fromtodiv2">
        <div>
          <h3>DEPARTURE</h3>
          <input type="date" className="date" />
        </div>
        {/* <div>
          <h3>RETURN</h3>
          <input placeholder="choose it" type="date" className="date" />
        </div> */}
        <div>
          <h3>TRAVLLER & CLASS</h3>
          <select name="" id="">
          <option value="">Select</option>

            <option value="">Economy</option>
            <option value="">Premium</option>

          </select>
        </div>
      </div>